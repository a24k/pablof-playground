name: pablof - milestone

on:
  pull_request:
  milestone:
    types: [created, opened, edited, closed, deleted]

jobs:
  pablof-milestone:
    runs-on: ubuntu-latest
    steps:
      - uses: a24k/pablof@b8a7a2c1681a7d08fd70ca9efbad0d34bbd0a1dc
        with:
          token: ${{ secrets.GH_TOKEN }}
