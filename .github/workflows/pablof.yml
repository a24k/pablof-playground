name: pablof

on:
  pull_request:
  issue:
    types: [opened]
  milestone:
    types: [created, opened, edited, closed, deleted]

jobs:
  pablof:
    runs-on: ubuntu-latest
    steps:
      - uses: a24k/pablof@create-sub-issue
        with:
          token: ${{ secrets.GH_TOKEN }}
